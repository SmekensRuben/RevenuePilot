// src/features/dashboard/dashboardService.js

// Hier kun je in de toekomst statistiekcalls etc. zetten.
// Voor nu een placeholder!
export function getWelcomeMessage(hotelName) {
  return `Welkom, ${hotelName}!`;
}
